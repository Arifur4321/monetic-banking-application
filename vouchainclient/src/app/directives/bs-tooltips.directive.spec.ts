import { BsTooltipsDirective } from './bs-tooltips.directive';

describe('BsTooltipsDirective', () => {
  it('should create an instance', () => {
    const directive = new BsTooltipsDirective();
    expect(directive).toBeTruthy();
  });
});
