import { TransactionRequestDTO } from './transaction-request-dto';

describe('TransactionRequestDto', () => {
  it('should create an instance', () => {
    expect(new TransactionRequestDTO()).toBeTruthy();
  });
});
