import { SimpleResponseDTO } from './simple-response-dto';

describe('SimpleResponseDto', () => {
  it('should create an instance', () => {
    expect(new SimpleResponseDTO()).toBeTruthy();
  });
});
