import { VoucherAllocationDTO } from './voucher-allocation-dto';

describe('VoucherAllocationDto', () => {
  it('should create an instance', () => {
    expect(new VoucherAllocationDTO()).toBeTruthy();
  });
});
